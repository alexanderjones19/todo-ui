import { createMuiTheme } from '@material-ui/core/styles';
// import colors from @material-ui/core/colors

const palette = {
  // construct your own color schemes
}

const theme = createMuiTheme({
  palette,
  // overrides can go here
});

export default theme;